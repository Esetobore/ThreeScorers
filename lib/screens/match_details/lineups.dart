import 'package:flutter/material.dart';

class Lineups extends StatelessWidget {
  const Lineups({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        SizedBox(
          child: Center(child: Text('No Lineups')),
        ),
      ],
    );
  }
}
